export function renderProfile(root: HTMLElement) {
  root.innerHTML = `
    <main class="page-profile">
      <h2>个人成就</h2>
      <p>账户系统接入中...</p>
      <p><a href="#/">← 返回</a></p>
    </main>
  `;
}
