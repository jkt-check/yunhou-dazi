export function renderAchievements(root: HTMLElement) {
  root.innerHTML = `
    <main class="page-achievements">
      <h2>成就墙</h2>
      <p>成就引擎待实现 (Task 28)</p>
      <p><a href="#/">← 返回</a></p>
    </main>
  `;
}
