export function renderSettings(root: HTMLElement) {
  root.innerHTML = `
    <main class="page-settings">
      <h2>设置</h2>
      <p>设置面板待实现 (Task 34)</p>
      <p><a href="#/">← 返回</a></p>
    </main>
  `;
}
